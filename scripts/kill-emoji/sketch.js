function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  
  background(0);
    
  noStroke();
  textSize(100);
//  text('🦌', mouseY, mouseX);
//  text('🐋', mouseY-10, mouseX);
//  text('🦭', mouseY, mouseX);
//  text('🦆', mouseY+10, mouseX);
  text('🔪', mouseX, mouseY);
  
    noCursor();

}