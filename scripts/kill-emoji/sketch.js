function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
    
  noStroke();
  textSize(100);
//  text('🦌', mouseY, mouseX);
//  text('🐋', mouseY-10, mouseX);
//  text('🦭', mouseY, mouseX);
//  text('🦆', mouseY+10, mouseX);
  text('🔪', mouseX, mouseY);
  
    noCursor();

}